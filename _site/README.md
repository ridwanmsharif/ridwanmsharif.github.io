ridwanmsharif.github.io
=========================

## Development

To set up and run this project locally, you'll need to have Ruby and Bundler installed.

1.  **Install dependencies:**

    ```bash
    bundle install
    ```

2.  **Run the local server:**

    ```bash
    make serve
    ```

    The site will be available at `http://localhost:4000`.
